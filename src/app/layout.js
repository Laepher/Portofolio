import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Raphael Rhinovaldi Wibisana | Back-End Web Developer Portfolio",
  description: "Portofolio profesional Raphael Rhinovaldi Wibisana, mahasiswa D3 Teknik Informatika Universitas Sebelas Maret dengan fokus pada Back-End Development dan AI Integration.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
